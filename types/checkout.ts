export type Billing = {
    firstName: string;
    lastName: string;
    postalCode: string;
    state: string;
    country: string;
};

export type Address = {
    fullName: string;
    streetAddress1: string;
    streetAddress2: string;
    city: string;
    countryArea: string;
    postalCode: string;
    country: string;
};

export type User = {
    email: string;
};
