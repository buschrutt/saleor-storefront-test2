mutation ProcessTransaction($transactionId: ID!) {
    transactionProcess(id: $transactionId) {
        transaction {
            id
        }
        data
        errors {
            field
            message
            code
        }
    }
}
